{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python manage.py migrate && python create_superuser.py && python populate_medicines.py && python manage.py collectstatic --noinput && gunicorn gemach.wsgi --log-file -",
    "restartPolicyType": "ON_FAILURE"
  }
}